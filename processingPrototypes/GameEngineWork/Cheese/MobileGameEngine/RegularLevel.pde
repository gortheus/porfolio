class RegularLevel extends GameLevel {
  RegularLevel(int type, int num, int successType, int successTarget, int failType, int failTarget) {
    super(type, num, successType, successTarget, failType, failTarget);
  }
}